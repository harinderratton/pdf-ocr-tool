services:
  - type: web
    name: pdf-ocr-backend
    env: node
    buildCommand: |
      apt-get update && apt-get install -y graphicsmagick ghostscript imagemagick
      npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
